# Preview build. Nothing here should ever be indexed.
User-agent: *
Disallow: /

# Inert while the Disallow above stands, and deliberately written now so
# that flipping this file at launch is a one-line change rather than a
# thing to remember.
#
# A sitemap index, listing six children split by locale and by whether the
# URL is a page or a post. Only the index goes here and into Search
# Console: a crawler reads the children from it, and submitting them
# individually as well would double every row in the coverage report.
# Built by app/sitemap.xml/route.ts from lib/site-urls.ts.
Sitemap: https://mikebastin.com/sitemap.xml
