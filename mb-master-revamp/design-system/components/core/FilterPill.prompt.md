Filter toggle above a filtered list. Only show a filter that would return something.

```jsx
<FilterPill on onClick={() => setFilter("all")}>All</FilterPill>
<FilterPill onClick={() => setFilter("training")}>Training</FilterPill>
```
