Last thing on every page, and the only contact block. Phone and email are visible in body copy, not hidden behind a form.

```jsx
<SiteFooter address />
<SiteFooter>{/* the /contact/ form drops in here */}</SiteFooter>
```
