One per page, mounted next to SiteNav. It writes data-theme on <html> and stores the choice under `mb-theme`.

```jsx
<ThemeToggle />
```

Anything you design has to work on both grounds, because this control can invert every band on the page at any moment.
