The structural argument on the homepage: 43 service pages became 13, and this is where the reader checks the claim.

```jsx
<ConsolidationDiagram initialOpen="local" />
```

One cluster open at a time. Counts read "43 to 13", never with a dash, because ranges use "to" site-wide.
